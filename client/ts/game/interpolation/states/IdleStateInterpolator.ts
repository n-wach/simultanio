import StateInterpolator from "../StateInterpolator";

export default class IdleStateInterpolator extends StateInterpolator {
    draw(ctx: CanvasRenderingContext2D): void {
    }

    interpolate(dt: number) {
    }
}